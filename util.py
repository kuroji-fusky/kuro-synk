from typing import Any


def open_file(fn: str):
    """Opens a file, simple enough for a simpleton to understand

    Args:
        fn (str): Filename
    """
    ...


def generate_dict_to_json_schema(schema: dict[str, Any]):
    ...
